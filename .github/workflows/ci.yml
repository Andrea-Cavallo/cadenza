name: CI

on:
  push:
<<<<<<< HEAD
    branches: [master, main, develop, "feature/**"]
  pull_request:
    branches: [master, main]

# Minimal permissions — each job elevates only what it needs.
permissions:
  contents: read

jobs:
  # ────────────────────────────────────────────────────────
  # 1. Test + Coverage
  # ────────────────────────────────────────────────────────
=======
    branches: [master, main, develop, feature/**]
  pull_request:
    branches: [master, main]

jobs:
  # ──────────────────────────────────────────────
  # 1. Test + Coverage
  # ──────────────────────────────────────────────
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e
  test:
    name: Test & Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
<<<<<<< HEAD
          fetch-depth: 0 # full history required by SonarCloud
=======
          fetch-depth: 0  # full history required by SonarCloud
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Vet
        run: go vet ./...

      - name: Test (race + coverage)
        run: go test ./... -race -count=1 -coverprofile=coverage.out -covermode=atomic

      - name: Coverage report
        run: go tool cover -func=coverage.out

      - name: Build
        run: CGO_ENABLED=0 go build -o /dev/null ./cmd/cadenza/

<<<<<<< HEAD
      - name: Cross-compile check (linux/amd64)
        run: GOOS=linux CGO_ENABLED=0 go build -o /dev/null ./cmd/cadenza/

=======
      - name: Cross-compile check (linux)
        run: GOOS=linux CGO_ENABLED=0 go build -o /dev/null ./cmd/cadenza/

      # Upload coverage for SonarCloud job
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out
          retention-days: 1

<<<<<<< HEAD
  # ────────────────────────────────────────────────────────
  # 2. Lint
  #
  # We install golangci-lint via `go install` instead of
  # using golangci-lint-action's pre-built binary.
  # Reason: the pre-built binary is compiled with a fixed Go
  # version (e.g. 1.24). When .golangci.yml targets go 1.26
  # the binary refuses to start with:
  #   "can't load config: the Go language version used to
  #    build golangci-lint is lower than the targeted Go version"
  # Compiling from source with the same toolchain eliminates
  # this mismatch permanently.
  # ────────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
=======
  # ──────────────────────────────────────────────
  # 2. Lint
  # ──────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Go must be set up BEFORE installing golangci-lint so that
      # "go install" compiles golangci-lint with the project's Go version.
      # Using the pre-built binary from golangci-lint-action would download
      # a binary compiled with an older Go toolchain, causing:
      #   "can't load config: the Go language version used to build
      #    golangci-lint is lower than the targeted Go version"
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

      - name: Run golangci-lint
        run: golangci-lint run ./...

  # ──────────────────────────────────────────────
  # 3. Security — vulnerability scan
  # ──────────────────────────────────────────────
  security:
    name: Security Scan
    runs-on: ubuntu-latest
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

<<<<<<< HEAD
      - name: Install golangci-lint from source
        run: go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

      - name: Run golangci-lint
        run: golangci-lint run ./...

  # ────────────────────────────────────────────────────────
  # 3. Security — vulnerability scan
  # ────────────────────────────────────────────────────────
  security:
    name: Security (govulncheck)
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        run: govulncheck ./...

  # ────────────────────────────────────────────────────────
  # 4. SonarCloud analysis
  #
  # SHA pinning note: @v4 / @v3 tags below are intentionally
  # left as version tags. Dependabot (.github/dependabot.yml)
  # is configured to open weekly PRs that replace these with
  # full commit SHA hashes, satisfying the OSSF Scorecard
  # "Pinned-Dependencies" check automatically.
  # ────────────────────────────────────────────────────────
=======
      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        run: govulncheck ./...

  # ──────────────────────────────────────────────
  # 4. SonarCloud analysis
  # ──────────────────────────────────────────────
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e
  sonar:
    name: SonarCloud
    runs-on: ubuntu-latest
    needs: test
<<<<<<< HEAD
    permissions:
      contents: read
=======
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e

    steps:
      - uses: actions/checkout@v4
        with:
<<<<<<< HEAD
          fetch-depth: 0
=======
          fetch-depth: 0  # full history required for blame annotations
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e

      - name: Download coverage artifact
        uses: actions/download-artifact@v4
        with:
          name: coverage
<<<<<<< HEAD
          path: .
=======
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

<<<<<<< HEAD
  # ────────────────────────────────────────────────────────
  # 5. Docker smoke test — only after test + lint pass
  # ────────────────────────────────────────────────────────
=======
  # ──────────────────────────────────────────────
  # 5. Docker image — only after test + lint pass
  # ──────────────────────────────────────────────
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e
  docker:
    name: Docker Build & Smoke Test
    runs-on: ubuntu-latest
    needs: [test, lint]
<<<<<<< HEAD
    permissions:
      contents: read
=======
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t cadenza:ci .

<<<<<<< HEAD
      - name: Smoke test (must produce 3 MIDI files)
        run: |
          mkdir -p output
          docker run --rm -v "$(pwd)/output:/app/output" cadenza:ci \
=======
      - name: Smoke test (offline mode — must produce 3 MIDI files)
        run: |
          mkdir -p output
          docker run --rm -v $(pwd)/output:/app/output cadenza:ci \
>>>>>>> c484ac349a230a4de9c6d72bbd2c39d4281a014e
            --bpm 122 --key Am --no-llm
          count=$(ls output/*.mid 2>/dev/null | wc -l)
          [ "$count" -eq 3 ] || (echo "Expected 3 MIDI files, got $count" && exit 1)
          echo "Docker smoke test passed: $count MIDI files generated"
